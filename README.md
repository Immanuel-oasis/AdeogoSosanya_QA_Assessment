# QA ASSESSMENT
## Manual & Automation Testing

The files represent each of the objectives stated in the document.

A local plugin was used in the application and an excel document can be generated by running `npx cypress run`

You can view the generated test result in the excel sheet in `cypress > test-docs > cypress/test-docs/test-documentation-latest.xls`